# Puerto del servidor Express (default: 8080)
PORT=8080

# URL del worker Python (default: http://localhost:5000)
WORKER_URL=http://localhost:5000

# JWT secrets (cambiar en producción)
JWT_SECRET=sky-loan-secret-change-in-production
JWT_REFRESH_SECRET=sky-loan-refresh-secret
